{
    "defaultAgent": "event-modeler",
    "agent": {
        "event-modeler": {
            "mode": "primary",
            "description": "Event Modeling facilitator",
            "disabledTools": [
                "eca__grep",
                "eca__directory_tree",
                "eca__editor_references",
                "eca__editor_definition",
                "eca__preview_file_change",
                "eca__write_file",
                "eca__edit_file",
                "eca__move_file",
                "eca__shell_command",
                "eca__git",
                "eca__view_image",
                "emcli_query",
                "emcli_validate",
                "emcli_resolve",
                "emcli_element",
                "emcli_connection",
                "emcli_slice",
                "emcli_timeline",
                "emcli_swimlane",
                "emcli_spec",
                "emcli_step",
                "emcli_placement",
                "emcli_wireframe"
            ],
            "prompts": {"chat": "${file:prompts/event-modeler.md}"},
            "toolCall": {
                "approval": {
                    "byDefault": "allow"
                }
            }
        },
        "em-explorer": {
            "mode": "subagent",
            "description": "Explore Event Model using emcli tools",
            "prompts": {"chat": "${file:prompts/em-explorer.md}"},
            "spawnableBy": ["event-modeler", "em-author", "wireframer"],
            "disabledTools": [
                "eca__grep",
                "eca__directory_tree",
                "eca__editor_references",
                "eca__editor_definition",
                "eca__preview_file_change",
                "eca__write_file",
                "eca__edit_file",
                "eca__move_file",
                "eca__shell_command",
                "eca__git",
                "eca__view_image",
                "eca__task",
                "eca__spawn_agent",
                "eca__ask_user",
                "eca__bg_job",
                "emcli_element",
                "emcli_connection",
                "emcli_slice",
                "emcli_timeline",
                "emcli_swimlane",
                "emcli_spec",
                "emcli_step",
                "emcli_placement",
                "emcli_wireframe"
            ],
            "toolCall": {
                "approval": {
                    "byDefault": "allow"
                }
            }
        },
        "em-author": {
            "mode": "subagent",
            "description": "Author Event Model pieces using emcli tools",
            "prompts": {"chat": "${file:prompts/em-author.md}"},
            "spawnableBy": ["event-modeler"],
            "disabledTools": [
                "eca__grep",
                "eca__directory_tree",
                "eca__editor_references",
                "eca__editor_definition",
                "eca__preview_file_change",
                "eca__write_file",
                "eca__edit_file",
                "eca__move_file",
                "eca__shell_command",
                "eca__git",
                "eca__view_image",
                "eca__task",
                "eca__ask_user",
                "eca__bg_job",
                "emcli_query",
                "emcli_validate",
                "emcli_resolve",
                "emcli_wireframe"
            ],
            "toolCall": {
                "approval": {
                    "byDefault": "allow"
                }
            }
        },
        "wireframer": {
            "mode": "subagent",
            "description": "Add wireframes to screen elements",
            "prompts": {"chat": "${file:prompts/wireframer.md}"},
            "spawnableBy": ["event-modeler"],
            "disabledTools": [
                "eca__grep",
                "eca__directory_tree",
                "eca__editor_references",
                "eca__editor_definition",
                "eca__preview_file_change",
                "eca__write_file",
                "eca__edit_file",
                "eca__move_file",
                "eca__shell_command",
                "eca__git",
                "eca__view_image",
                "eca__task",
                "eca__ask_user",
                "eca__bg_job",
                "emcli_query",
                "emcli_validate",
                "emcli_resolve",
                "emcli_element",
                "emcli_connection",
                "emcli_slice",
                "emcli_timeline",
                "emcli_swimlane",
                "emcli_spec",
                "emcli_step",
                "emcli_placement"
            ],
            "toolCall": {
                "approval": {
                    "byDefault": "allow"
                }
            }
        }
    },
    "customTools": {
        "emcli_connection" : {
            "description" : "Manage connections and field derivations between elements.\nVerbs: add (--from --to [--id]), add-derivation (--connection --target --from), remove (--connection), remove-derivation (--connection --target)",
            "command" : "emcli connection {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "add-derivation", "remove", "remove-derivation" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_element" : {
            "description" : "Manage elements (commands, events, read models, screens, automations) and their fields.\nVerbs: add (--name --element-type [--id]), add-field (--element --name --type [--cardinality] [--optional] [--subfield-of]), add-origin (--element --field --origin), context (--element --new-context), delete (--element), image (--element --url), remove-field (--element --name), remove-origin (--element --field), rename (--element --new-name), rename-field (--element --name --new-name), swimlane (--element --lane)",
            "command" : "emcli element {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "add-field", "add-origin", "context", "delete", "image", "remove-field", "remove-origin", "rename", "rename-field", "swimlane" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_placement" : {
            "description" : "Manage element placements within timeline slices.\nVerbs: add (--slice --element [--id]), remove (--slice --element), reorder (--slice --element [--position] [--before] [--after])",
            "command" : "emcli placement {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "remove", "reorder" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_query" : {
            "description" : "Read-only structural query over the Event Model. Answers relational questions - e.g. which slices an element is placed in, which elements an event derives from - and returns only the matching part of the model. Use `emcli_resolve` first if you only have names; this tool never writes.\n\nA query is a root followed by stages separated by `|`, applied left-to-right over a collection.\n\nROOT (required, first): an entity kind - timeline | swimlane | slice | element | specification | step - or a single entity as `kind:id` (e.g. `element:42`).\n\nRELATION (a target kind, singular or plural accepted; placements and connections are edges, never roots):\n  timeline -> slice\n  slice -> timeline\n  slice -> specification\n  specification -> slice\n  specification -> step\n  step -> specification\n  step -> element\n  element -> step\n  element -> swimlane\n  swimlane -> element\n  slice -> element   (via placement; project {index})\n  element -> slice   (via placement; project {index})\n  element -> element   [outgoing]   (via connection; project {derivations})\n  element -> element   [incoming]   (via connection; project {derivations})\n\nEDGE PROJECTION `{field,...}`: attach the association record under its key (placement or connection), e.g. `slice:7 | elements {index}` gives each element plus `:placement {:index n}`.\n\nSTAGES - a pipeline of stages, one per `|`-separated segment of the query:\n  where <field> <op> <value>\n  order <field>, or order -<field> to descend\n  select <field>,<field>,...\n  count\n  limit <n>\n  distinct\n\nOPERATORS - <op> is exactly one of these, standing alone: write it straight after the field and before the value, with no `=` of its own:\n  =    equals\n  !=   not equals\n  ~    regex match\n  in   set membership - a list, as `in (a,b)` or `in a,b` (braces are not used)\n\nFIELD: a stage names either a row's display field - id, kind (the entity CATEGORY, e.g. `element`), name, breadcrumb, or a projected edge's key - or an attribute the landed entity declares. So `element | select name,element_type,context,fields` reads an element's own field names, which is how you learn what the field operations address a field by (`fields` is the recursive list, subfields included). Beware that `kind` is the CATEGORY: what an element IS is its `element_type` (command | event | read_model | screen | automation), and what a slice is, is its `slice_type`. A slice's `index` is its position in its timeline (0 = first), so `timeline:3 | slices | order index | select title,index` lists a timeline's slices in display order - the field to check after reordering a slice. A swimlane's `index` is likewise its position among the swimlanes (the rows of every timeline), and a step's its position in its specification. The set is per kind, and an unknown field name is REJECTED naming what that row does carry - never silently an empty result.\n\nExamples:\n  timelines | slice | where status = done | select id,title\n  timeline:3 | slices | order index | select title,index\n  element | where element_type in (command,event) | select id,name\n  element:42 | slice\n  slice:7 | elements {index} | where element_type = command\n  element:42 | outgoing {derivations}\n  element | select name,fields\n\nAn invalid stage, kind, relation or field name returns an error naming the valid alternatives. Run `emcli query --relations` for the live list.",
            "command" : "emcli query --query '{{query}}'",
            "schema" : {
                "properties" : {
                    "query" : {
                        "type" : "string",
                        "description" : "A pipeline, e.g. `element:42 | slice` or `slice:7 | elements {index}`. Root first, then `|`-separated stages; see the tool description for the grammar and relation vocabulary."
                    }
                },
                "required" : [ "query" ]
            }
        },
        "emcli_resolve" : {
            "description" : "Resolve one or more element/timeline/slice names to their integer ids. Use before authoring commands when you have names but not ids.",
            "command" : "emcli resolve --queries '{{queries}}'",
            "schema" : {
                "properties" : {
                    "queries" : {
                        "type" : "string",
                        "description" : "comma-separated name[:kind_hint] entries, e.g. \"Baz:slice,Snaz\"; kind_hint is one of timeline|swimlane|slice|element|specification and only ranks candidates, never filters them"
                    }
                },
                "required" : [ "queries" ]
            }
        },
        "emcli_slice" : {
            "description" : "Manage slices (vertical time segments) within a timeline.\nVerbs: add (--timeline --title --slice-type [--before] [--after] [--id]), delete (--slice), rename (--slice --new-title), reorder (--slice [--position] [--before] [--after]), status (--slice --new-status), type (--slice --new-slice-type)",
            "command" : "emcli slice {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "delete", "rename", "reorder", "status", "type" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_spec" : {
            "description" : "Manage specifications attached to slices.\nVerbs: add (--slice --title [--id]), delete (--spec), rename (--spec --new-title)",
            "command" : "emcli spec {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "delete", "rename" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_step" : {
            "description" : "Manage steps and examples within a specification.\nVerbs: add (--spec --clause --element --index [--id]), add-example (--step --field-name --field-value), error (--spec --error-name --index [--id]), expect-empty (--step --value), remove (--step), remove-example (--step --field-name), rename-error (--step --new-error-name)",
            "command" : "emcli step {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "add-example", "error", "expect-empty", "remove", "remove-example", "rename-error" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_swimlane" : {
            "description" : "Manage swimlanes (horizontal rows) in the event model.\nVerbs: add (--name --index [--id]), delete (--lane), rename (--lane --new-name), reorder (--lane --new-index)",
            "command" : "emcli swimlane {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "delete", "rename", "reorder" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_timeline" : {
            "description" : "Manage timelines (vertical swim-lane columns) in the event model.\nVerbs: add (--title [--id]), delete (--timeline), rename (--timeline --new-title)",
            "command" : "emcli timeline {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add", "delete", "rename" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        },
        "emcli_validate" : {
            "description" : "Run Event Model validation and return all warnings and errors.",
            "command" : "emcli validate",
            "schema" : {
                "properties" : { },
                "required" : [ ]
            }
        },
        "emcli_wireframe" : {
            "description" : "Manage wireframe node trees on screen elements and display them.\nVerbs: add-node (--element --tag [--text] [--parent]), add-node-before (--element --before --tag [--text]), delete-node (--element --node), set-attr (--element --node --attr --value), set-text (--element --node --text), show (--element)",
            "command" : "emcli wireframe {{verb}} {{args}}",
            "schema" : {
                "properties" : {
                    "verb" : {
                        "type" : "string",
                        "enum" : [ "add-node", "add-node-before", "delete-node", "set-attr", "set-text", "show" ]
                    },
                    "args" : {
                        "type" : "string",
                        "description" : "Space-separated --flag value pairs for the chosen verb. Optional flags may be omitted."
                    }
                },
                "required" : [ "verb" ]
            }
        }
    }
}
